using System.Windows.Controls;

namespace Translator.UI.Controls;

/// <summary>Small indeterminate progress ring; the look lives in Surfaces.xaml (animates only while visible).</summary>
public sealed class Spinner : Control
{
}
